describe('Component: Home', () => {
  let $componentController;

  beforeEach(angular.mock.module('App.pages.Home'));

  beforeEach(angular.mock.inject(( _$componentController_) => {
    $componentController =  _$componentController_;
  }));
});
